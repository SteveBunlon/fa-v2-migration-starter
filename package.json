{
  "name": "fa-v2-starter",
  "version": "1.0.0",
  "description": "A sample project to onboard using agent-nodejs",
  "main": "index.js",
  "scripts": {
    "build": "$(npm bin)/tsc",
    "build:watch": "$(npm bin)/tsc --watch",
    "start": "$(npm bin)/nodemon --delay 250ms --watch '../*/dist/' --exec node dist/index.js"
  },
  "dependencies": {
    "@forestadmin/agent": "^1.0.0",
    "@forestadmin/datasource-sql": "^1.0.0",
    "dotenv": "^16.0.1",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "typescript": "^4.8.2"
  },
  "author": "steveb",
  "license": "ISC"
}
